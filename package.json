{
  "scripts": {
    "serve": "vue-cli-service serve",
    "dev": "vue-cli-service build --target wc --name vmc src/components/*.vue",
    "build": "vue-cli-service build --mode production --target wc"
  },
  "devDependencies": {
    "@vue/cli-service": "^3.0.0-rc.5"
  }
}
